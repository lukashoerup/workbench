[Unit]
Description=Apply repo changes to this machine every 10 minutes

[Timer]
# 2 min after boot so the network is up; then every 10 min. Persistent so a box
# that was asleep still reconciles once it wakes.
OnBootSec=2min
OnUnitActiveSec=10min
Persistent=true

[Install]
WantedBy=timers.target
